# 任务进展记录

> 更新：2026-08-24
> 状态：**完成。网络部分恢复（PyPI / nodejs.org 可达，Docker Hub 仍被拒），已按复验清单重建并全量复验通过；
> 另补齐 `submit_job → Celery send_task` 异步投递链路（含 e2e 验证）。**

## 1. 本次任务

- 为 openwiki-server 生成 Docker 构建脚本，参考 `/home/open-ikc`，将 **HAProxy 作为基础代理组件
  构建到同一镜像**（Python 引擎 + Node openwiki 内核 + HAProxy 单镜像栈）。

## 2. 已交付文件

| 文件 | 说明 |
| --- | --- |
| `Dockerfile` | 单阶段：`python:3.12-slim` + apt HAProxy + Node 22（官方 tarball，绕开 Docker Hub node 镜像）+ npm 全局 `openwiki@0.3.3` + `pip install .`；非 root uid 1000；只暴露 HAProxy 入口（8080 HTTP / 50052 gRPC / 8404 stats） |
| `docker/haproxy.cfg` | HAProxy 配置模板（envsubst 渲染 stats 账号）：HTTP `:8080` → `127.0.0.1:18011`（httpchk /health）、gRPC `:50052` TCP 透传、stats `:8404` |
| `docker/entrypoint.sh` | 入口脚本：`api` 角色（uvicorn + gRPC 仅回环 + HAProxy 前台，就绪探测 fail-fast，TERM/INT 优雅停机）/ `worker` 角色（Celery） |
| `docker/.env.example` | compose 环境变量模板（端口、stats 密码、LLM 内核配置） |
| `docker-compose.yml` | `app` + `redis:7.0-alpine` + `worker`（worker/redis 走 `--profile worker`） |
| `scripts/build_docker.sh` | 构建脚本（版本自动取 `package.json`，`--no-cache` 支持，`IMAGE_TAG` 可覆盖） |
| `scripts/docker_smoke.sh` | 冒烟脚本（7 项断言，`--force-build` 支持） |
| `.dockerignore` / `.gitignore` | 新增；`.gitignore` 增加 `.env` |
| `README.md` | 新增「Docker 部署（单镜像，含 HAProxy）」章节 |

## 3. 已验证结果

### 2026-08-24（复验 + 投递链路补齐）

- 镜像 `openwiki-server:1.0.0` **重建成功**（Docker Hub 仍被拒，但 `python:3.12-slim` 基础镜像已缓存；
  apt/npm/node 层全部走构建缓存，仅 pip 层重新联网，PyPI 已恢复可达）。
- 冒烟 **7/7 通过（默认端口 18011 / 50052 / 8404）**：`/health`、wiki create、build 建页（total=1）、
  gRPC 经 HAProxy 真实调用、stats 凭据 200/401、引擎仅回环、非 root uid 1000。
- Celery worker 就绪并连上 redis（`celery@... ready.`，`redis-cli ping` → PONG）。
- **补齐异步投递链路**：`submit_job` 登记后按任务字段白名单（剔除 `async` 等 HTTP 专有字段）
  经 `celery_app.send_task` 投递；broker 不可达时守卫式保持 pending，可 `run_job` 手动执行。
- **e2e 验证通过**：经 HAProxy 提交 `async=1` build → job `pending → active → success`，
  页面由 worker 实际构建（`mode=rule, total=1`）并回写 job 表。
- 本地 `pytest tests -q`：**18 passed**（新增投递成功/失败守卫两条用例）。

### 2026-08-20（初次交付）

- 镜像 `openwiki-server:1.0.0` **构建成功**（含 Node 22.14.0 + openwiki 0.3.3 + haproxy）。
- HAProxy 配置经 `haproxy:3.0-alpine` `haproxy -c` **校验零告警**。
- 冒烟 **7/7 通过**：`/health`、wiki create、build 建页（total=1）、gRPC 经 HAProxy 真实调用、
  stats 默认凭据 200 / 错误 401、引擎 18011 仅回环（容器 IP 被拒）、非 root uid 1000。
- Celery worker 启动并连上 redis（`celery@... ready.`）。
- 本地 `pytest tests -q`：**16 passed**。
- 修复真实缺陷：镜像内 worker 因缺少 `redis` 包崩溃 → `pyproject.toml` 依赖补充 `redis>=5.0`。

## 4. 已知限制 / 待办

- **网络受限**：Docker Hub（registry-1.docker.io）仍被拒；PyPI / nodejs.org 已恢复可达。
  构建依赖本地缓存基础镜像，新增/失效层仍需 apt / npm / pip 网络（当前仅 pip 层需要）。
- **端口冲突**：宿主机 8404 被运行中的 `open-ikc-app-1` 占用；冒烟改用
  `OPENWIKI_HTTP_PORT=18111 OPENWIKI_GRPC_PORT=50552 HAPROXY_STATS_PORT=8414`。
  （2026-08-24 复验时 `open-ikc-app-1` 已退出，8404 空闲，默认端口冒烟通过。）
- `redis` 固定为 `7.0-alpine`（本机缓存版本；标准 tag `7-alpine` 拉取失败）。

## 5. 网络恢复后的复验清单

```bash
cd /home/sharkyai/openwiki-server

# 1. 全量重建（拉取基础镜像/依赖）
bash scripts/build_docker.sh --no-cache

# 2. 默认端口冒烟（需先处理 8404 占用，或覆盖端口）
OPENWIKI_HTTP_PORT=18111 OPENWIKI_GRPC_PORT=50552 HAPROXY_STATS_PORT=8414 \
  bash scripts/docker_smoke.sh --force-build

# 3. 验证 Celery worker（redis 拉取成功后）
OPENWIKI_HTTP_PORT=18111 OPENWIKI_GRPC_PORT=50552 HAPROXY_STATS_PORT=8414 \
  docker compose --profile worker up -d
docker compose logs worker | tail -5    # 期望：celery@... ready.

# 4. 本地测试
python -m pytest tests -q
```

## 6. 可选后续

- ~~补 `submit_job` → `celery_app.send_task` 投递~~ **已完成（2026-08-24）**。
- 若生产环境可访问 Docker Hub，可改回多阶段 `node:22-bookworm-slim`（注释已保留设计意图）。
- 冒烟脚本可再加「异步 build e2e」断言（submit → worker 消费 → success），作为第 8 项。
